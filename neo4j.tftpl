#!/bin/sh
touch /tmp/lb_fqdn

echo "Welcome to another edition of thunderdome" >> /tmp/lb_fqdn
echo "The Load Balancer's FQDN = ${lb_fqdn}" >> /tmp/lb_fqdn
echo "Graph Database Version will be ${gdb_ver}" >> /tmp/lb_fqdn